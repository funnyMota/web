# if判断

if()
{

}
else if()
{

}
else
{

}

## 规则
1. 如果某个条件满足,则忽略后面
2. else if可以有多个
3. else 有1个有0个
4. else可以换行 可以不换行
5. 如果只有一条语句,可以不写花括号(不推荐)

## 补充

1. 用户输入: prompt("提示文本");

该表达式返回结果有三种
- null:表示用户点了取消
- 用户输入的字符串:表示用户点击了确定,得到用户输入的结果

返回类型:null或字符串

2. 将字符串转换为数字: +字符串
3. 得到随机数
Math.random() 表示返回0~1的随机数,无法取到1