# 正则表达式

正则表达式是国际标准,跨越语言

正则表达式是一个规则,用于验证字符串

> 在字符串中必须出现abc

## 基础

1. 字面量匹配
直接匹配

2. 特殊字符
```js

. 任意字符
^ 开始匹配
$ 结束匹配


```
3. 转义符
```
\n 换行
\r 回车
\t 制表符
\s 空白字符
\b 单词边界
\d 数字
\w 匹配数字字母下划线
\u 匹配unicode
```
转义符可以将特殊字符转义

4. 字符集
```
[字符集]
[^字符] 对字符取反
```

5. 量词
```
* 匹配 0 个或多个
+ 匹配 1 个或多个
? 匹配 0 个或 1 个
{n} 匹配n个
{n,} 匹配>=n个
{n,m} 匹配n~m个
```

6. 或者
多个规则之间```|```匹配一个规则
多个规则之间```&```都匹配

## js的应用

js中,正则表达式表现为一个对象,该对象是通过构造函数RegExp

### 创建正则对象

1.字面量模式
2.构造函数模式

### 正则成员
- global 开局全局匹配
- ignoreCase 忽略大小写
- source 目前规则
- test验证字符串是否满足规则

> 正则表达式,默认情况下,使用贪婪模式
> 在量词后,加上?,表示进入非贪婪模式

### 实例成员

### 静态成员

## 进阶
