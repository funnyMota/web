# 文本元素

HTML5中支持的元素：HTML5元素

## h
- 标题h1~h6
## p
- 段落
## span
- 无语义元素
> 块级元素（独占一行），行级元素（不会独占）
> 根据web3c标准描述到了H5已经抛弃了这个概念
## pre
- 预格式化文本元素
- 空白折叠（换行，空格）会被折叠为一个空格
- 在pre元素内部出现的内容，会被以源代码的格式显示
- 在网页中显示代码
- 有个默认的CSS属性
- 通常外面套code元素