# 文本元素

HTML5 中支持的文本相关元素。

## `h1` ~ `h6`

- 标题元素

## `p`

- 段落

## `span`

- 无语义元素

> 传统概念：块级元素（独占一行）、行级元素（不独占）。  
> 根据 W3C 的描述，在 HTML5 语境下已不再强调该分类。

## `pre`

- 预格式化文本元素
- 默认不会发生空白折叠（换行、空格会按源码显示）
- 常用于在网页中展示代码，通常会搭配 `code` 元素