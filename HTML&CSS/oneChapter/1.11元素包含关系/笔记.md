# 元素包含关系

## 以前的说法

- 块级元素独占一行；行级元素不独占
- 块级元素可以包含行级元素
- 行级元素不可以包含块级元素
- `a` 元素除外

## 现在的理解

元素的包含关系由元素的内容类别决定。

### 例如

- `h1` 中不能包含 `p`

## 总结

1. 容器元素几乎可以包含任何元素
2. `a` 元素中几乎可以包含任何元素
3. 某些元素有固定的子元素
4. 标题元素和段落元素不能相互嵌套，并且不能包含容器元素