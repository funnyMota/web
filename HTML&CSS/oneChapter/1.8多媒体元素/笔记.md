# 多媒体元素

- `video`：视频
- `audio`：音频

## video

布尔属性：可以只写属性名，不写值（此时取值等于属性名）。

- `controls`：控制控件
- `autoplay`：自动播放
- `muted`：静音播放
- `loop`：循环播放

### `source` 子元素

通常把具体资源写在 `source` 里。

## audio

与 `video` 用法基本一致。

## 兼容性

1. 旧版本的浏览器不支持
2. 不同的浏览器支持的音视频格式不同
